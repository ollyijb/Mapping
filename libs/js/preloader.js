$(window).on('load', function () { // makes sure the whole site is loaded
    $('#preloader').delay(15000).fadeOut(500);
    $('#status').delay(15000).fadeOut(500);
    // will fade out the white DIV that covers the website.
    //$('body').delay(200).show();
});